
@{
    ViewBag.Title = "Login";
}
<div class="navbar navbar-inverse navbar-fixed-top">
    <div class="container" style="position:relative;">
        <div class="navbar-header">
            @using (Html.BeginForm("Login", "Home", FormMethod.Post, new { @class = "form-inline navbar-right" }))
            {
                <div class="nav-form form-group">
                    <input type="text" name="login" id="inputLogin" class="form-control" placeholder="Login" />
                    <button type="submit" id="loginBtn" class="btn btn-default">Log in</button>
                </div>
            }
        </div>
    </div>
</div>

<div>

</div>

